'use strict';

/**
 * service-sub-page-one router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::service-sub-page-one.service-sub-page-one');
