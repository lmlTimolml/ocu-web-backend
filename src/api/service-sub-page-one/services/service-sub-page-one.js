'use strict';

/**
 * service-sub-page-one service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::service-sub-page-one.service-sub-page-one');
